# INCLUSAO DE BIBLIOTECAS

use "solarisIo.sol"
use "solarisLib.sol"
use "directory/myLib.sol"

##
    comentario de bloco
    onde pode ser incluido uma sequencia de caracteres
##


# DECLARACAO DE VARIAVEIS

integer x;
string name = "Bruna";
string oldName = "Andre";
string address;
float y;
integer num = 5 + 9;
integer total = 0;


floatNumber = numberDecimal;

integer expression = (5 + 5);

function (integer): sumOfSquares receive (x, y, z) {
    integer sum = 5;

    # RETORNO DE FUNCAO

    return sum;
}

function (integer): getMaxNumber receive (x, y) {
    integer maxNumber;

    choose ( x >= y ) {
        maxNumber = x;
    } otherwise {
        maxNumber = y;
    }

    return maxNumber;
}

function (float): getFloatNumberExample receive () {
    return 5.0;
}

function (string): getHelloWorld receive () {
    return "Hello, World!";
}

call getHelloWorld();
