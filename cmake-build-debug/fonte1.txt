integer x;
string name = "Bruna";
string oldName = "Andre";
string address;
float y;

integer num = 5 + 9;
integer total = 0;

y = (5 + 9) * (9 + (9/2) * 9) && 1;
oldName = name;

name = "Joao";
x = 5;
oldName = name;
y = 0.939393;

choose ( oldName == Name ) {
}

choose ( oldName == Name ) {
    choose ( x >= y ) {
        x = y;
    } otherwise {
        y = x;
    }
} otherwise {
    x = 5;
}

integer counter = 0;

while (counter <= 8 && (counter != 9)) {
    counter = counter + 1;

    choose (counter == 8) {

    }
}

loop (integer i = 3) until ( i <= 5) {
    x = 90;

    loop (integer i = 3) until ( i <= 5) {
        x = 90;
    }

    loop (integer i = 3) until ( i <= 5) {
        x = 90;
    }
}

# Este e um exemplo de codigo correto em Solaris com tipos de variaveis inspirados em C

##
   Este e um comentario de bloco.
   Pode incluir multiplas linhas e
##

show "Ola Mundo!";
show name;

read name;

function (integer): sumOfSquares receive (integer a, integer b) {
    integer sum = a * a + b * b;

    return sum;
}
