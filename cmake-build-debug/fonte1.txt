# INCLUSAO DE BIBLIOTECAS

use "solarisIo.sol"

##
    comentario de bloco
    onde pode ser incluido uma sequencia de caracteres
##

# DECLARACAO DE VARIAVEIS

integer n1 = 5;
float n2 = 2.5;
string str = "Exemplo";

integer total;
float x;
string name;

# ATRIBUICOES

total = 6;
x = 2 + (2 / 2) + 1;
x = 2.5;
name = "Camile";

# OPERACOES CONDICIONAIS

choose ( n1 == total ) {
}

choose ( n1 == total ) {
    choose ( n1 >= total ) {
        show n1;
    } otherwise {
        show n2;
    }
} otherwise {
    show n1;
    show n2;
}

# LOOP 1: WHILE

integer counter = 0;
while (counter <= 8 && (counter != 9)) {
    counter = counter + 1;

    choose (counter == 8) {

    }
}

# LOOP 2: LOOP - UNTIL

loop (counter = 3) until ( counter <= 5) {
    x = 95.4;
}

# ESCRITA DE DADOS

show "Ola Mundo!";
show name;

# LEITURA DE DADOS

read name;

# DELCARACAO DE FUNCAO

integer a;
integer b;

function (integer): changeNumberValue receive (a, b) {
    a = b;

    # RETORNO DE FUNCAO

    return a;
}

# CHAMADA DE FUNÇÃO

call changeNumberValue(a, b);
