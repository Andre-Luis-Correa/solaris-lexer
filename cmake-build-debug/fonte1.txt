# INCLUSAO DE BIBLIOTECAS

use "solarisIo.sol"
use "solarisLib.sol"
use "directory/myLib.sol"

##
    comentario de bloco
    onde pode ser incluido uma sequencia de caracteres
##

# DECLARACAO DE VARIAVEIS

float x;
string name = "Bruna";
string oldName = "Andre";
string address;
float y;
integer num = 5 + 9;
integer total = 0;

# ATRIBUICOES

y = (5 + 9) * (9 + (9/2) * 9) && 1;
oldName = name;
name = "Joao";
x = 5.0;
oldName = name;
y = 0.939393;

# OPERACOES CONDICIONAIS

choose ( oldName == Name ) {
}

choose ( oldName == Name ) {
    choose ( x >= y ) {
        x = y;
    } otherwise {
        y = x;
    }
} otherwise {
    x = 7.8;
}

# LOOP 1: WHILE

integer counter = 0;
while (counter <= 8 && (counter != 9)) {
    counter = counter + 1;

    choose (counter == 8) {

    }
}

# LOOP 2: LOOP - UNTIL

integer i;

loop (i = 3) until ( i <= 5) {
    x = 90.0;
}

# ESCRITA DE DADOS

show "Ola Mundo!";
show name;

# LEITURA DE DADOS

read name;

# DELCARACAO DE FUNCAO

integer a;
integer b;

function (integer): changeNumberValue receive (a, b) {
    a = b;

    # RETORNO DE FUNCAO

    return a;
}

call changeNumberValue(a, b);
